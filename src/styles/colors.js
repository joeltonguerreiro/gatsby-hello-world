const primaryColor = '#5570ff'

const secondaryColor = '#aea7fd';

const backgroundPrimaryColor = '#eaeaea';

const backgroundSecondaryColor = '#fafafa';


export const colors = {
    primaryColor,
    secondaryColor,
    backgroundPrimaryColor,
    backgroundSecondaryColor
}